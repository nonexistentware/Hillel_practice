const firstArray = [2, 9, 6];
const secondArray = [1, 12, 7];

function arrayConcat(firstArray, secondArray) {
    let result = firstArray.concat(secondArray);

    return result;
}

console.log(arrayConcat(firstArray, secondArray));